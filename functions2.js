/*
FUNCTION TASK2:---------------
Write a function that accepts two number arguments, adds them together and returns the summed up value.
Then call/invoke the function and store it's value into a variable.
and then log the value returned by the function to the variable.
*/
function taskTwo(number2, number3){
	
	return number2 + number3;

}
const numero2 = taskTwo(7, 14);

console.log(numero2);